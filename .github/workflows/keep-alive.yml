name: Keep Supabase Active (Enhanced)

on:
  schedule:
    # Run every 2 days at 3:00 AM UTC to prevent 7-day auto-pause
    # More frequent than the original 3-day schedule for better reliability
    - cron: '0 3 */2 * *'

  # Allow manual trigger from GitHub Actions UI
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
    - name: Enhanced Supabase Database Activity
      continue-on-error: false  # Fail if database is unreachable
      run: |
        echo "🏓 Enhanced Supabase keep-alive starting..."
        echo "📅 Run time: $(date -u)"
        echo "🔄 Frequency: Every 2 days"
        echo "🎯 Purpose: Prevent 7-day auto-pause on free tier"
        echo ""

        # Test 1: Basic API connectivity
        echo "🌐 Testing API connectivity..."
        api_response=$(curl -s -o /dev/null -w "%{http_code}" \
          -H "apikey: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
          "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/")

        echo "API Response: $api_response"

        if [ "$api_response" -eq 401 ]; then
          echo "✅ API responding (401 expected without specific table access)"
        elif [ "$api_response" -eq 200 ]; then
          echo "✅ API responding successfully"
        else
          echo "⚠️ Unexpected API response: $api_response"
        fi
        echo ""

        # Test 2: Actual database query - Count users (this creates real DB activity)
        echo "🗄️ Testing database activity with actual query..."
        users_response=$(curl -s -w "%{http_code}" \
          -H "apikey: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
          "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/users?select=count" \
          -o /tmp/users_response.txt)

        users_code=$(echo "$users_response" | tail -c 4)
        echo "Users query response: $users_code"

        # Test 3: Check strengths table (reference data)
        echo "📊 Testing strengths table access..."
        strengths_response=$(curl -s -w "%{http_code}" \
          -H "apikey: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
          "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/strengths?select=count&limit=1" \
          -o /tmp/strengths_response.txt)

        strengths_code=$(echo "$strengths_response" | tail -c 4)
        echo "Strengths query response: $strengths_code"

        # Test 4: Check email logs table (shows system is active)
        echo "📧 Testing email logs table..."
        logs_response=$(curl -s -w "%{http_code}" \
          -H "apikey: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" \
          "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}/rest/v1/email_logs?select=count&limit=1" \
          -o /tmp/logs_response.txt)

        logs_code=$(echo "$logs_response" | tail -c 4)
        echo "Email logs query response: $logs_code"
        echo ""

        # Summary
        echo "📋 Database Activity Summary:"
        echo "   • API connectivity: $api_response"
        echo "   • Users table query: $users_code"
        echo "   • Strengths table query: $strengths_code"
        echo "   • Email logs query: $logs_code"
        echo ""

        # Success criteria
        success_count=0
        [ "$api_response" -eq 200 ] || [ "$api_response" -eq 401 ] && success_count=$((success_count + 1))
        [ "$users_code" = "200" ] || [ "$users_code" = "401" ] || [ "$users_code" = "403" ] && success_count=$((success_count + 1))
        [ "$strengths_code" = "200" ] || [ "$strengths_code" = "401" ] || [ "$strengths_code" = "403" ] && success_count=$((success_count + 1))
        [ "$logs_code" = "200" ] || [ "$logs_code" = "401" ] || [ "$logs_code" = "403" ] && success_count=$((success_count + 1))

        echo "✅ Successful connections: $success_count/4"

        if [ "$success_count" -ge 2 ]; then
          echo "✅ Keep-alive successful: Database activity registered"
          echo "🛡️ Supabase project will remain active"
        else
          echo "⚠️ Warning: Limited database connectivity"
          echo "📞 Manual check may be needed"
        fi

    - name: Ping Production Application
      continue-on-error: true
      run: |
        echo ""
        echo "🌐 Testing production application..."

        # Ping the actual production URL
        if [ -n "${{ secrets.PRODUCTION_URL }}" ]; then
          app_response=$(curl -s -o /dev/null -w "%{http_code}" \
            "${{ secrets.PRODUCTION_URL }}")
          echo "Production app response: $app_response"

          if [ "$app_response" -eq 200 ]; then
            echo "✅ Production application is healthy"
          else
            echo "⚠️ Production app returned: $app_response"
          fi
        else
          # Use known production URL
          app_response=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://stronger.tinymanager.ai")
          echo "App response: $app_response"

          if [ "$app_response" -eq 200 ]; then
            echo "✅ Application is responding"
          else
            echo "⚠️ Application returned: $app_response"
          fi
        fi

    - name: Summary Report
      run: |
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🎯 ENHANCED KEEP-ALIVE COMPLETE"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "📅 Completed: $(date -u)"
        echo "⏰ Next run: In 2 days"
        echo "🔄 Schedule: Every 2 days at 3:00 AM UTC"
        echo "🛡️ Protection: Against 7-day Supabase auto-pause"
        echo "📊 Method: Multiple database queries for real activity"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"